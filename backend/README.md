# PulseShop Backend

Placeholder — the backend is specified in [`../backend.md`](../backend.md) and will be
built in a later phase. Until then, the frontend runs entirely against the mock
service layer in `frontend/src/services/mock/`.
